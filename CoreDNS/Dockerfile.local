# Use the official CoreDNS image as the base
FROM coredns/coredns:latest

# Copy the Corefile into the container
COPY Corefile /etc/coredns/

# Expose both TCP and UDP ports 53 for DNS queries
EXPOSE 53/tcp 53/udp

# Set CoreDNS as the entrypoint
ENTRYPOINT ["/coredns"]
